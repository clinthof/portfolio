import React from 'react';
import { FooterWrapper } from '../../Styles';

const Footer: React.FC = () => {
    return (
        <FooterWrapper>
            Footer component
        </FooterWrapper>
    )
}

export default Footer;
